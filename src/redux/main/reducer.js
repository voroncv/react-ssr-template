const initialState = {
  testArray: [],
}

export const TEST_ACTION = 'project/main/TEST_ACTION'

export default function main(state = initialState, action) {
  switch (action.type) {
    
    default: {
      return state
    }
  }
}
